# General rules

## General Code Standards

- Write as few comments as possible. Only if it's really necessary.
- When you got command "ri" (run instructions) FIND all such comments  LLM: <LLM INSTRUCT> with "grep -rnw  -e 'LLM INSTRUCT'". You MUST DO the instructions and then remove this comments. You MUST NOT do anything else.
