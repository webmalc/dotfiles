- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']
- link: {}
- shell:
    -
      command: "curl -L http://get.oh-my.fish | fish 2>&1; true"
      stdout: true
      stderr: true
    -
      command: 'crontab -l > mycron; echo "0 12 * * * /home/webmalc/Projects/dotfiles/task/notifications.py" >> mycron; crontab mycron; rm mycron'
      stdout: true
      stderr: true