- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]
- link: {}
- shell:
    - command: "curl -L http://get.oh-my.fish | fish 2>&1; true"
      stdout: true
      stderr: true
